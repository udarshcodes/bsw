services:
  - type: web
    name: quantumlens
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server
    envVars:
      - key: GROQ_API_KEY
        sync: false
